export enum MarkFontStyle {
  BOLD = "bold",
  ITALIC = "italic",
  UNDERLINE = "underline",
}

export enum MarkTextAlign {
  ALIGN_LEFT = "left",
  ALIGN_CENTER = "center",
  ALIGN_RIGHT = "right",
  ALIGN_JUSTIFY = "justify",
}

export enum MarkEditHistory {
  UNDO = "undo",
  REDO = "redo",
}
